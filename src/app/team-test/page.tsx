import TeamTest from "@/components/sections/TeamTest";

export const metadata = {
  title: "Team Test Layout | Diversity Resources Limited",
  description: "Testing a new layout for the team section.",
};

export default function TeamTestPage() {
  return (
    <main>
      <TeamTest />
    </main>
  );
}
